(ns helda-server.listeners.invoker
  (:require
    [clj-http.client :as client]
    [helda.storages.entity-listeners :find-listeners-by-entity-id]
    )
  )

(defn find-action-route [action entity]

  )

(defn fire-action [action src-entity dst-entity action-ctx]

  )
